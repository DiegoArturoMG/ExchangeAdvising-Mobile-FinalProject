<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exchangeadvisingapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.SendMessage"></activity>
        <activity android:name=".Activities.ViewMessages" />
        <activity android:name=".Activities.ModifyStudent" />
        <activity
            android:name=".Activities.StudentMenu"
            android:noHistory="true" />
        <activity
            android:name=".Activities.Login"
            android:noHistory="true" />
        <activity android:name=".Activities.Signup" />
        <activity
            android:name=".Activities.Splash"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>